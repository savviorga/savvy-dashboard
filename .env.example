# Savvy Dashboard – variables de entorno de ejemplo
# Copia este archivo a .env y rellena los valores.
# Nunca subas .env al repositorio.

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
# URL base de la aplicación (ej. en producción)
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# APIs / Backend (cuando se conecte a un servidor)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_API_URL=http://localhost:4000/api
# API_SECRET_KEY=

# -----------------------------------------------------------------------------
# Opcional: analytics, monitoreo
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# -----------------------------------------------------------------------------
# PostgreSQL (elige una opción)
# -----------------------------------------------------------------------------
# Opción 1: URI completa (si la usas, las variables DB_* se ignoran)
# DATABASE_URL=postgresql://usuario:contraseña@host:5432/nombre_base

# Opción 2: Variables por separado
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_contraseña
DB_NAME=db_savvy_2
# Segunda base de datos (gastos n8n). Mismas credenciales, otro nombre.
DB_NAME_GASTOS=db_savvy
# SSL (true = usar SSL; pon false solo para PostgreSQL local sin SSL)
DB_SSL=true

